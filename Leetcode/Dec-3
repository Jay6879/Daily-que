class Solution {
    public String addSpaces(String s, int[] spaces) {
        StringBuilder sb = new StringBuilder(s);

        int count = 0;

        for(int num:spaces){
            sb.insert(num+count," ");
            count++;
        }

        return sb.toString();

    }
}
